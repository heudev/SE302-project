import { Typography } from "@mui/material";

export function WeeklyTable() {
    return (
        <Typography variant="body1" sx={{ textAlign: 'center', color: 'text.secondary', mt: 2 }}>
            Weekly Table will be displayed here.
        </Typography>
    );
}
